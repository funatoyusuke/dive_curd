<p id="notice"><%= notice %></p>


<table class="table table-hover">
  <thead>
    <th>会社名</th>
    <th>住所</th>
    <th>電話番号</th>
    <th>ファックス</th>
    <th>Webサイト</th>
    <th></th>
  </thead>

  <tbody>
    <tr>
    <td><%= @company.name %></td>
    <td><%= @company.address %></td>
    <td><%= @company.tel %></td>
    <td><%= @company.fax %></td>
    <td><%= @company.url %></td>
    <td>
    <%= link_to '編集', edit_company_path(@company), class:'btn btn-default' %>
    <%= link_to '部署一覧へ', company_groups_path(@company), class:'btn btn-default' %>
    <%= link_to '会社一覧へもどる', companies_path, class:'btn btn-default' %>
    </td>

    </tr>
  </tbody>
</table>

    <div>
   <div class="panel panel-title"> <h4>メモ</h4></div>
    <%= @company.comment %>
    </div>
<br>

     <div class="panel panel-title"><h4>コメント</h4></div>

  <div id="comments_area">
    <%= render partial: 'comments/index', locals: { comments: @comments, company: @company } %>
  </div>

  <%= render partial: 'comments/form', locals: { comment: @comment, company: @company } %>
</div>



