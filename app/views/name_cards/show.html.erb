<p id="notice"><%= notice %></p>
<table class="table table-hover">
   <thead>
     <th>名前</th>
     <th>住所</th>
     <th>電話番号</th>
     <th>ファックス</th>
     <th>Webサイト</th>
   </thead>
 
   <tbody>
     <tr>
       <td><%= @name_card.name %></td>
       <td><%= @name_card.address %></td>
       <td><%= @name_card.tel %></td>
       <td><%= @name_card.fax %></td>
       <td><%= @name_card.url %></td>
     </tr>
   </tbody>
 </table>
  
  <div class="card" style="width: 20rem;">
    <div class="card-body">
      <%= image_tag @name_card.image.url if @name_card.image.url  %>
    </div>
  </div>

<%= link_to '編集', edit_company_group_user_name_card_path(@company, @group, @user, @name_card), class:'btn btn-primary' %> 
<%= link_to '名刺一覧に戻る', company_group_user_name_cards_path, class:'btn btn-primary' %>



