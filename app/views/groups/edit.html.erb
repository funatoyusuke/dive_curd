<h1>編集画面</h1>

<%= render 'form' %>

<%= link_to '詳細', company_group_path(@company, @group), class:'btn btn-primary' %>
<%= link_to '部署一覧へもどる', company_groups_path(@company, @group), class:'btn btn-primary' %>
